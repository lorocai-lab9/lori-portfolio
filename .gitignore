# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Env / secrets
.env
.env.local
.env.*.local

# Dependencies (in case any tooling shows up later)
node_modules/

# Build output
dist/
build/
.cache/

# 3D assets — uncompressed source models (large). Commit optimized
# (Draco + WebP) versions only.
*.glb
*.gltf
*.fbx
*.bin
!assets/robot-arm.optimized.glb

# Quarantined for manual deletion — must never ship (see _DELETE_ME/README)
_DELETE_ME/

# Local pre-downscale backups of committed images (large, redundant)
assets/_originals/
